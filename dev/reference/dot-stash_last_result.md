# Save most recent results to search path

Save most recent results to search path

## Usage

``` r
.stash_last_result(x)
```

## Arguments

- x:

  An object.

## Value

NULL, invisibly.

## Details

The function will assign `x` to `.Last.tune.result` and put it in the
search path.
