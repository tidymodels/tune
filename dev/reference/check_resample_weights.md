# Check fold weights in rset objects

Check fold weights in rset objects

## Usage

``` r
check_resample_weights(x)
```

## Arguments

- x:

  An rset object.

## Value

`NULL` invisibly, or error if weights are invalid.
