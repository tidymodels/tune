> dummy_template(model, verbose, tune)
ranger_recipe <- 
  recipe(formula = Sepal.Width ~ ., data = iris) 

ranger_model <- 
  rand_forest(mtry = tune(), min_n = tune(), trees = 1000) %>% 
  set_mode("regression") %>% 
  set_engine("ranger") 

ranger_workflow <- 
  workflow() %>% 
  add_recipe(ranger_recipe) %>% 
  add_model(ranger_model) 

set.seed(27246)
ranger_tune <-
  tune_grid(ranger_workflow, resamples = rlang::abort("add your rsample object"), 
    grid = 20)


NULL

